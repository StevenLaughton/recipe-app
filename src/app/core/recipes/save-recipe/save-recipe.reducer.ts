import { Action, createReducer, on } from '@ngrx/store';
import * as SaveRecipeActions from './save-recipe.actions';

// export const saveRecipeFeatureKey = 'saveRecipe';

// export interface State {

// }

// export const initialState: State = {

// };


// export const reducer = createReducer(
//   initialState,

//   on(SaveRecipeActions.saveRecipe, state => state),
//   on(SaveRecipeActions.loadSaveRecipesSuccess, (state, action) => state),
//   on(SaveRecipeActions.loadSaveRecipesFailure, (state, action) => state),

// );

